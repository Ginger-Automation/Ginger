    <Page x:Class="Ginger.Run.RunSetActions.RunSetActionScriptEditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
	Title="RunSetActionScriptEditPage">

    <Grid Background="{StaticResource $BackgroundColor_LightGray}">
       
        <StackPanel>
            <Label HorizontalAlignment="Left" Style="{StaticResource @LabelStyle}">Script File Name:</Label>
            <StackPanel Orientation="Horizontal" >
                <TextBox x:Name="ScriptFileNameTextBox" Width="500" Style="{StaticResource @TextBoxStyle}" ></TextBox>
                <Button x:Name="BrowseButton" Click="BrowseButton_Click" Style="{StaticResource @InputButtonStyle}" Width="50" Margin="5,0,0,0" VerticalAlignment="Center">Select</Button>
            </StackPanel>
            <Label HorizontalAlignment="Left" Foreground="Red" Style="{StaticResource @LabelStyle}">Script Type: on windows only VBS supported, on linux only Shell supported.</Label>
        </StackPanel>

    </Grid>
</Page>
