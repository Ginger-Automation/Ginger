    <Page x:Class="Ginger.Actions.Java.ActJavaElementEditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="ActJavaElementEditPage">

    <Grid Background="{StaticResource $BackgroundColor_LightGray}">
        <StackPanel>
            <Label Style="{StaticResource @LabelStyle}" FontWeight="Bold">Operation to Perform:</Label>
            <ComboBox x:Name="ControlActionComboBox" SelectionChanged="ControlActionComboBox_SelectionChanged" Style="{StaticResource @InputComboBoxStyle}"></ComboBox>

            <Label Style="{StaticResource @LabelStyle}" Margin="0,10,0,0">Wait for Idle Option:</Label>
            <ComboBox x:Name="WaitforIdleComboBox" Style="{StaticResource @InputComboBoxStyle}"></ComboBox>
        </StackPanel>
    </Grid>
</Page>