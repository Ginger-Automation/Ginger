    <Page x:Class="GingerPACTPlugIn.ActionEditPages.Start"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GingerPACTPlugIn.ActionEditPages"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="Start">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="Row1" Height="25"/>
            <RowDefinition x:Name="Row2" Height="25"/>
            <RowDefinition x:Name="Row3" Height="25"/>
            <RowDefinition x:Name="Row4" Height="25"/>
            <RowDefinition x:Name="Row5" Height="25"/>
            <RowDefinition x:Name="Row6" Height="25"/>
        </Grid.RowDefinitions>

        <Label Content="Start a PACT Mock Service:" Grid.Row="0" Grid.Column="0"  HorizontalAlignment="Left" FontFamily="Open Sans" FontSize="16" Foreground="#20334f"/>
        <Label Content="Port:"  Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left" FontFamily="Open Sans" FontSize="14" Foreground="#20334f" />
        <TextBox x:Name="Port" Grid.Row="2" Grid.Column="0" Text="1234"   BorderBrush="#20334f" FontSize="12" />
        <CheckBox x:Name="DynamicPortCheckBox" Grid.Row="3" Grid.Column="0" ToolTip="If checked Ginger will configure free port for you" Content="Use Dynamic Port" Margin="0,10,0,0" Checked="DynamicPortChecked" Unchecked="DynamicPortUnchecked" HorizontalAlignment="Left" IsChecked="false" />
    </Grid>
</Page>
