<Page x:Class="Ginger.RunSetLib.CreateCLIWizardLib.AutoRunWizardOptionsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:GingerCore="clr-namespace:GingerCore;assembly=GingerCore"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CLIOptionsPage">

    <Grid Background="{StaticResource $BackgroundColor_LightGray}">
        <StackPanel Margin="10">
            <Label HorizontalAlignment="Left" Style="{StaticResource $SectionHeaderLabelStyle}">General Options:</Label>

            <CheckBox x:Name="xDownloadsolutionCheckBox" Style="{StaticResource $CeckBoxStyle}" Margin="5" Checked="xDownloadsolutionCheckBox_Checked" IsChecked="false" Unchecked="xDownloadsolutionCheckBox_Unchecked">Download/Update Solution from source control</CheckBox>

            <CheckBox x:Name="xGingerRunEXEWindowShow" Margin="5" Checked="xGingerRunEXEWindowShow_Checked" Unchecked="xGingerRunEXEWindowShow_Unchecked" Style="{StaticResource $CeckBoxStyle}">
                <CheckBox.Content>
                    <GingerCore:ucTextDicResource Text="Show [RunSet] execution window while running (relevent only in case running on Windows)"/>
                </CheckBox.Content>
            </CheckBox>

            <CheckBox x:Name="xRunAnalyzerCheckBox" Style="{StaticResource $CeckBoxStyle}" Margin="5" Checked="xRunAnalyzerCheckBox_Checked" Unchecked="xRunAnalyzerCheckBox_Unchecked" IsChecked="True" >Run Analyzer before execution</CheckBox>

        </StackPanel>
    </Grid>
</Page>
