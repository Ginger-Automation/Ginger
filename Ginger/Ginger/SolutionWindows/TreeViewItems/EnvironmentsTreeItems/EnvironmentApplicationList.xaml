<Page x:Class="Ginger.SolutionWindows.TreeViewItems.EnvironmentsTreeItems.EnvironmentApplicationList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:Ginger="clr-namespace:Ginger"
       xmlns:UserControls="clr-namespace:Amdocs.Ginger.UserControls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Add Application">

    <Grid Background="{StaticResource $BackgroundColor_White}">

        <Ginger:ucGrid x:Name="AppsGrid"  ShowSearch="Hidden" ShowTitle="Collapsed" ShowAdd="Collapsed" ShowRefresh="Collapsed" ShowDelete="Collapsed" ShowClearAll="Collapsed" ShowEdit="Collapsed" ShowUpDown="Collapsed"/>

        <UserControls:ucButton x:Name="xAddApplicationToSolution" 
                               ButtonType="RoundTextAndImageButton" 
                               VerticalAlignment="Top" 
                               Margin="0,5,10,0"
                               HorizontalContentAlignment="Right"
                               ButtonText="Add Application" 
                               ButtonImageType="Add" 
                               ButtonFontImageSize="15" 
                               ButtonImageWidth="15" 
                               ButtonImageHeight="15" 
                               ButtonStyle="{StaticResource $RoundTextAndImageButtonStyle}" 
                               IsEnabled="True"
                               Click="AddApplicationToSolution"
                               />


       
    </Grid>
</Page>
