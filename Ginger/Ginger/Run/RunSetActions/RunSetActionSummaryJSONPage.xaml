<Page x:Class="Ginger.Run.RunSetActions.RunSetActionSummaryJSONPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Ginger.Run.RunSetActions"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RunSetActionSummaryJSONPage">

    <Grid x:Name="pageGrid" Background="{StaticResource $BackgroundColor_LightGray}">

        <StackPanel>
            <Label HorizontalAlignment="Left">Note: The JSON Summary Report will be saved under the Solution "ExecutionResults" folder</Label>
            <!--<Label HorizontalAlignment="Left" Style="{StaticResource @LabelStyle}">Target JSON summary folder:</Label>
            <StackPanel Orientation="Horizontal" >
                <TextBox x:Name="TargetFolderTextBox" Width="500" Style="{StaticResource @TextBoxStyle}" ></TextBox>
                <Button x:Name="BrowseButton" Style="{StaticResource @InputButtonStyle}" Width="50" Margin="5,0,0,0" VerticalAlignment="Center">Select</Button>
            </StackPanel>-->
        </StackPanel>
    </Grid>
</Page>
