<Page x:Class="Ginger.External.Katalon.ImportPOMSummaryWizardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Ginger.External.Katalon"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ImportPOMSummaryWizardPage"
      Background="{StaticResource $BackgroundColor_White}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="auto" />
            <RowDefinition
                Height="auto" />
            <RowDefinition
                Height="auto" />
        </Grid.RowDefinitions>
        <TextBlock 
            Grid.Row="0"
            Style="{StaticResource $CoolTabTextBlockStyle}"
            FontSize="18"
            Text="Summary of Import"
            FontWeight="SemiBold"/>
        <StackPanel
            Orientation="Horizontal"
            Grid.Row="1"
            Margin="16 10 0 0">
            <TextBlock
                Text="Total Page Object Models (POMs) Imported:"
                Style="{StaticResource $DetailsTextBlockStyle}"
                FontSize="13"
                FontWeight="DemiBold"/>
            <TextBlock
                Padding="8 0 0 0"
                x:Name="ImportCountTextBlock"
                Style="{StaticResource $DetailsTextBlockStyle}"
                FontSize="13" />
        </StackPanel>
        <StackPanel
            x:Name="ImportLocationStackPanel"
            Margin="16 10 0 0"
            Orientation="Horizontal"
            Grid.Row="2">
            <TextBlock
                Text="Import Location:"
                Style="{StaticResource $DetailsTextBlockStyle}"
                FontSize="13"
                FontWeight="DemiBold" />
            <TextBlock
                Padding="8 0 0 0"
                Width="400"
                x:Name="ImportLocationTextBlock"
                TextWrapping="Wrap"
                Style="{StaticResource $DetailsTextBlockStyle}"
                FontSize="13" />
        </StackPanel>
    </Grid>
</Page>
