<UserControl x:Class="Amdocs.Ginger.UserControls.ChatbotWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:usercontrols="clr-namespace:Amdocs.Ginger.UserControls"
             Height="600" Width="320" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Label Background="Black" FontWeight="Bold" HorizontalContentAlignment="Center" Foreground="White" Content="Ask Lisa"/>
            <Image HorizontalAlignment="Right"  Source="{DynamicResource $Close-Window.png}" />

        </Grid>
        <Grid Grid.Row="1">
            <ScrollViewer>
                <StackPanel x:Name="chatPanel">
                    <!-- Dummy chat messages -->
                    <TextBlock Text="Lisa: Hello!" Margin="5"/>
                    <TextBlock Text="You: Hi there!" Margin="5"/>
                </StackPanel>
            </ScrollViewer>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="5">
                <!-- User input -->
                <TextBox Width="300">
                    <TextBox.Template>
                        <ControlTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions></Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0"></TextBox>
                                <Button HorizontalAlignment="Right" Width="50" Grid.Column="1" Background="Transparent" BorderThickness="0">
                                    <usercontrols:ImageMakerControl x:Name="xSend" Grid.Row="1" ImageType="SendIcon" Width="50" Foreground="LightGray" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Button>
                            </Grid>
                        </ControlTemplate>
                    </TextBox.Template>
                </TextBox>
                <!-- text block working part
                <TextBox x:Name="txtInput" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Width="200" Margin="5"/>
                <Button Click="SendMessage" Width="30" Height="32" Margin="5">
                        <usercontrols:ImageMakerControl x:Name="xSend" Grid.Row="1" ImageType="SendIcon" Width="50" Foreground="LightGray" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                -->
            </StackPanel>
        </Grid>
    </Grid>

</UserControl>
