    <UserControl x:Class="Amdocs.Ginger.UserControls.ImageMakerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:fa="http://schemas.fontawesome.com/icons/svg"
             xmlns:foa="http://schemas.fontawesome.com/icons/fonts"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="150">
    <Border BorderThickness="0" CornerRadius="90" x:Name="ImageMakerBorder" Background="Transparent" BorderBrush="Transparent">
        <Grid>
            <fa:ImageAwesome x:Name="xFAImage" Visibility="Collapsed" Icon="Regular_AddressBook" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <foa:FontAwesome x:Name="xFAFont" Visibility="Collapsed" Icon="Regular_AddressBook" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <Image x:Name="xStaticImage" Visibility="Collapsed"></Image>

            <Viewbox x:Name="xViewBox" Stretch="Fill" Visibility="Collapsed">
                <Viewbox.Effect>
                    <DropShadowEffect/>
                </Viewbox.Effect>
                <Path Data="M 2,0 L 4,2 2,4 0,2 Z" 
              Width="4" Height="4"
              Fill="LightBlue" />
            </Viewbox>

            <TextBlock x:Name="xLabel" Text="Get"   
                       FontSize="16"   
                       HorizontalAlignment="Center"   
                       VerticalAlignment="Center"   
                       Margin="0,5,0,0"   
                       Foreground="White"   
                       Visibility="Collapsed" />
        </Grid>
    </Border>
</UserControl>
