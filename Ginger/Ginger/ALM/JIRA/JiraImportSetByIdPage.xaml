    <Page x:Class="Ginger.ALM.JIRA.JiraImportSetByIdPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:Ginger="clr-namespace:Ginger" xmlns:GingerCore="clr-namespace:GingerCore;assembly=GingerCore"
      xmlns:UCControl="clr-namespace:Ginger.UserControlsLib"
      mc:Ignorable="d" 
      Height="90" Width="500"
	  Title="Jira Import Test Set By Id">


    <Grid Background="{StaticResource $BackgroundColor_LightGray}">
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="90*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1">
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Label Style="{StaticResource @InputFieldLabelStyle}" VerticalAlignment="Top" Content="Test Set Id:"/>
                <TextBox x:Name="txtTestSetId"  Margin="0,0,28,0" Width="150" Height="25"  Style="{StaticResource @TextBoxStyle}"/>
            </StackPanel>
        </StackPanel>
        <StackPanel Grid.Row="2">
            <Label Name="lblStatus" Content=""  HorizontalAlignment="Left"/>
        </StackPanel>
    </Grid>
</Page>
