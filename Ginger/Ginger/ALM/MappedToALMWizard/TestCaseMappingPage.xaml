<Page x:Class="Ginger.ALM.MappedToALMWizard.TestCaseMappingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Ginger.ALM.MappedToALMWizard"
      xmlns:Ginger="clr-namespace:Ginger"
      xmlns:UserControlsLib="clr-namespace:Ginger.UserControlsLib" 
      xmlns:UserControls="clr-namespace:Amdocs.Ginger.UserControls" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TestCaseMappingPage">

    <Grid Margin="0,0,5,5" Background="{StaticResource $BackgroundColor_LightGray}">
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="70*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="1,1,1,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50*"/>
                <ColumnDefinition Width="50*"/>
            </Grid.ColumnDefinitions>
            <Label x:Name="xGingerBFName" Content="Ginger: Selected BF" HorizontalAlignment="Center" Margin="5,5,0,0" VerticalAlignment="Top"/>
            <Label x:Name="xALMTSName" Content="ALM: Selected TS" Grid.Column="2" HorizontalAlignment="Center" Margin="5,5,0,0" VerticalAlignment="Top"/>
        </Grid>
        <Grid Grid.Row="1" Margin="1,1,1,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40*"/>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition Width="40*"/>
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="gingerAGCbx" HorizontalAlignment="Stretch" Margin="5,5,0,0" VerticalAlignment="Top" Width="Auto"/>
            <ComboBox x:Name="almTCCbx" Grid.Column="2" HorizontalAlignment="Stretch" Margin="5,5,0,0" VerticalAlignment="Top" Width="Auto"/>
            <UserControls:ucButton x:Name="xMapTestCases" Grid.Column="1" ButtonType="RoundTextAndImageButton" VerticalAlignment="Top" HorizontalAlignment="Center" ButtonText="Map" ButtonImageType="MapSigns" ButtonFontImageSize="15" ButtonImageWidth="15" ButtonImageHeight="15"  Click="Button_Click" ButtonStyle="{StaticResource $RoundTextAndImageButtonStyle}" Visibility="Visible"/>
        </Grid>
        <Ginger:ucGrid ScrollViewer.CanContentScroll="True" x:Name="xMapActivityGroupTestCaseGrid" Grid.Row="2" Title="Mapped Tests Case" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ShowRefresh="Collapsed" ShowCopy="Collapsed" ShowPaste="Collapsed" ShowCut="Collapsed" ShowEdit="Collapsed" ShowClearAll="Collapsed" ShowUpDown="Visible" ShowDelete="Collapsed" ShowAdd="Collapsed"/>
    </Grid>
</Page>
