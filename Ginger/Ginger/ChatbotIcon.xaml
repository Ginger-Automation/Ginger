<UserControl x:Class="Amdocs.Ginger.UserControls.ChatbotIcon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:usercontrols="clr-namespace:Amdocs.Ginger.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="50">
    <Button x:Name="xChatbotIcon" Width="50" Height="50" Click="ChatbotIcon_Click" Background="Blue" Foreground="White" BorderThickness="0" BorderBrush="Transparent" >
        <Button.Template>
            <ControlTemplate TargetType="Button">
                <Grid>
                    <Ellipse Fill="MediumPurple" />
                    <usercontrols:ImageMakerControl x:Name="xChatbot" Grid.Row="1" ImageType="Chatbot" Width="50" Foreground="LightGray" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </ControlTemplate>
        </Button.Template>
    </Button>
</UserControl>
