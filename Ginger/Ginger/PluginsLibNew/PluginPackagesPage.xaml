    <Page x:Class="GingerWPF.PluginsLib.PluginPackagesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GingerWPF.PluginsLib"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="PluginsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="17*"/>
            <RowDefinition Height="133*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <Button x:Name="AddButton" Content="Add" Click="AddButton_Click"/>
        </StackPanel>
        
        <DataGrid x:Name="MaingGrid" CanUserAddRows="False" Grid.Row="1"/>

    </Grid>
</Page>
