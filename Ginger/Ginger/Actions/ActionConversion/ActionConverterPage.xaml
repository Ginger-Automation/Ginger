    <Page x:Class="Ginger.Actions.ActionConversion.ActionConverterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:Ginger="clr-namespace:Ginger"
      xmlns:GingerCore="clr-namespace:GingerCore;assembly=GingerCore"
      xmlns:usercontrols="clr-namespace:Amdocs.Ginger.UserControls"
      mc:Ignorable="d" 
      Height="800" Width="600"
      Title="ActionConverterPage">

    <Grid Background="{StaticResource @Skin1_ColorC}">

        <Grid.RowDefinitions>
            <RowDefinition Height="250"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="250"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="140"/>
            <RowDefinition Height="170"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <Ginger:ucGrid x:Name="grdGroups" Grid.Row="0" ShowTagsFilter="Collapsed" IsReadOnly="True" ShowAdd="Collapsed" ShowDelete="Collapsed" ShowClearAll="Collapsed"  ShowCut="Collapsed" ShowRefresh="Collapsed" ShowUndo="Collapsed" ShowUpDown="Collapsed" ShowEdit="Collapsed">
            <Ginger:ucGrid.Title>
                <GingerCore:ucTextDicResource Text="List of Activities"/>
            </Ginger:ucGrid.Title>
        </Ginger:ucGrid>

        <GridSplitter x:Name="gridsSplitter" ResizeDirection="Rows" Height="0" Width="Auto"  HorizontalAlignment="Stretch" VerticalAlignment="Bottom"  Style="{StaticResource @HorizontalGridSplitterStyle}" Margin="0,0,0,24" Grid.ColumnSpan="3"/>

        <Button x:Name="btnShowConvertibleActionTypes" Click="btnShowConvertibleActionTypes_Click"   Height="Auto" Grid.Row="2" Content="View Convertible Action Types" Width="182"  HorizontalAlignment="Stretch"  Style="{StaticResource @InputButtonStyle}" VerticalAlignment="Stretch" Margin="180,1,208,8" />

        <Ginger:ucGrid x:Name="gridConvertibleActions" Title="Convertible Action Types" ShowTagsFilter="Collapsed" Grid.Row="3" Grid.Column="0" IsReadOnly="True" ShowAdd="Collapsed" ShowDelete="Collapsed" ShowClearAll="Collapsed" ShowCut="Collapsed" ShowRefresh="Collapsed" ShowUndo="Collapsed" ShowUpDown="Collapsed" ShowEdit="Collapsed"/>

        <DockPanel x:Name="conversionConfigLblPanel"  HorizontalAlignment="Stretch" LastChildFill="true" Margin="0,0,0,0" VerticalAlignment="Stretch" Grid.Row="4">
            <Border DockPanel.Dock="Top" Visibility="{Binding  ElementName=lblTitle, Path=Visibility}" Background="{Binding ElementName=lblTitle, Path=Background}" 
                    BorderThickness="1" BorderBrush="{StaticResource @Skin1_ColorA}"  >
                <StackPanel>
                    <Label Style="{DynamicResource @ucTitleStyle_Light}"  HorizontalContentAlignment="Center">
                        <Label.Content >
                            <GingerCore:ucTextDicResource Text="Conversion Configurations"/>
                        </Label.Content>
                    </Label>
                </StackPanel>
            </Border>
        </DockPanel>
        <DockPanel x:Name="conversionConfigRadBtnPanel"  Grid.Row="5" HorizontalAlignment="Stretch" LastChildFill="true" Margin="0,0,0,0"  >
            <Border DockPanel.Dock="Top" Visibility="{Binding  ElementName=ToolsTray, Path=Visibility}" BorderThickness="1" BorderBrush="{StaticResource @Skin1_ColorA}"  >
                <StackPanel x:Name="RowSelectorPanel"  >
                    <Label x:Name="lbl" Content="Add new action to :" FontSize="12" Margin="35,10,0,0" Style="{StaticResource @InputFieldLabelStyle}"/>
                    <RadioButton Name="radNewActivity" Content="New Activity" Tag="radDifferentActivity" Margin="40,10,0,0"/>
                    <RadioButton Name="radSameActivity" Content="Same Activity" Tag="radSameActivity" Margin="40,10,0,0" IsChecked="True"/>
                    <CheckBox Name="chkDefaultTargetApp" Content="Set default target application to modified activities" Margin="40,20,0,0" Checked="chkDefaultTargetApp_Checked" Unchecked="chkDefaultTargetApp_Unchecked" IsChecked="True"/>
                    <ComboBox Name="cmbTargetApp" Style="{StaticResource @InputComboBoxStyle}" Width="180"  Margin="300,-20,200,0" />
                    <Button x:Name="btnRefresh" usercontrols:ImageMaker.Content="Refresh" ToolTip="Refresh" Click="btnRefresh_Click" Margin="510,-23,200,0" Width="22" Style="{StaticResource @InputImageButtonStyle_NoBorder}">                        
                    </Button>
                    
                </StackPanel>
            </Border>
        </DockPanel>
        
        <Button x:Name="btnConvert" Click="btnConvertAction_Click" Height="Auto" Grid.Row="6"  Content="Convert" Width="182"  HorizontalAlignment="Stretch"  Style="{StaticResource @InputButtonStyle}" VerticalAlignment="Stretch" Margin="180,20,208,127" />
        <Label x:Name="lblConversionStatus" Content="Please wait while the actions are converting..." FontWeight="Bold" FontSize="12" Margin="35,-100,0,0" Style="{StaticResource @InputFieldLabelStyle}" Visibility="Collapsed" Grid.Row="7" />
    </Grid>
</Page>
