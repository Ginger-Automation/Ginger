    <Page x:Class="Ginger.ApplicationModelsLib.POMModels.AddEditPOMWizardLib.POMLearnConfigWizardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:Ginger="clr-namespace:Ginger"     
      xmlns:GingerCore="clr-namespace:GingerCore;assembly=GingerCore"    
      xmlns:agents="clr-namespace:Ginger.Agents"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="600"
      Title="Learn Config">

    <Grid Background="{StaticResource $BackgroundColor_LightGray}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="100*"/>
        </Grid.RowDefinitions>

        <Grid Margin="10,10,0,0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left" Content="Page Elements Settings Method:"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <RadioButton x:Name="xManualElementConfigurationRadioButton" GroupName="ElementConfigRadioButtonsGroup" Style="{StaticResource @InputRadioButtonStyle}" IsChecked="False" Checked="xAutomaticElementConfigurationRadioButton_Checked" Content="Manual Setup"></RadioButton>
                <RadioButton x:Name="xAutomaticElementConfigurationRadioButton" GroupName="ElementConfigRadioButtonsGroup"  Style="{StaticResource @InputRadioButtonStyle}" IsChecked="True" Checked="xAutomaticElementConfigurationRadioButton_Checked" Margin="10,0,0,0" Content="Automatic Learning"></RadioButton>
            </StackPanel>
        </Grid>

        <agents:ucAgentControl x:Name="xAgentControlUC" Grid.Row="1"></agents:ucAgentControl>

        <Expander x:Name="xAutoMapElementTypesExpander" Margin="0,20,0,0" Grid.Row="2" Visibility="Visible" IsExpanded="False" >
            <Expander.Header>
                <Label x:Name="PropertiesExpanderLabel" Style="{StaticResource @HorizontalExpanderLightLabelStyle}" FontWeight="Normal" FontSize="12">
                    <Label.Content>
                        <GingerCore:ucTextDicResource Text="Elements Types to Auto Map"/>
                    </Label.Content>
                </Label>
            </Expander.Header>
            <Expander.Content>
                <Ginger:ucGrid x:Name="xAutoMapElementTypesGrid" ShowTitle="Collapsed" ShowAdd="Collapsed" ShowClearAll="Collapsed" ShowUpDown="Collapsed" ShowRefresh="Collapsed" ShowEdit="Collapsed"  ShowDelete="Collapsed" />
            </Expander.Content>
        </Expander>

       
    </Grid>
</Page>
