<Window x:Class="Ginger.Drivers.DriversWindows.MobileDriverWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:UserControls="clr-namespace:Amdocs.Ginger.UserControls"
        mc:Ignorable="d"
        Title="UpdatedAppiumDriverWindow" Height="600" Width="800"
        MinHeight="600" MinWidth="300" WindowStyle="SingleBorderWindow"
        WindowStartupLocation="CenterScreen" Closing="Window_Closing" SizeChanged="Window_SizeChanged"
        Topmost="False" >

    <Grid x:Name="xWindowGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="300*"/>
            <ColumnDefinition x:Name="xConfigurationsCol" Width="250"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>

        <!--Main area top buttons-->
        <Frame x:Name="xMainTopFrame" Grid.Row="0" Grid.Column="0" Margin="0,0,0,0" Background="Black"/>
        <UserControls:ucButton x:Name="xRefreshButton" ButtonType="ImageButton" HorizontalAlignment="Center" VerticalAlignment="Bottom" ButtonFontImageSize="24" ButtonImageType="Refresh" Click="xRefreshButton_Click" ToolTip="Refresh Device View" Margin="0,0,0,0"/>
        <UserControls:ucButton x:Name="xConfigurationsBtn" ButtonType="ImageButton" HorizontalAlignment="Right" VerticalAlignment="Bottom" ButtonFontImageSize="20" ButtonImageType="Config" Click="xConfigurationsBtn_Click" ToolTip="Show Configurations" Margin="0,0,5,0" />
        <UserControls:ucButton x:Name="xPinBtn" ButtonType="ImageButton" HorizontalAlignment="Right" VerticalAlignment="Bottom" ButtonFontImageSize="20" ButtonImageType="Pin" Click="xPinBtn_Click" ToolTip="Dock Window" Margin="0,0,30,0" />
        <UserControls:ucButton x:Name="xPortraiteBtn" ButtonType="ImageButton" HorizontalAlignment="Left" VerticalAlignment="Bottom" ButtonFontImageSize="23" ButtonImageType="Mobile" Click="xOrientationBtn_Click" ToolTip="Switch to Portrait" Margin="10,0,0,0" Visibility="Collapsed"/>
        <UserControls:ucButton x:Name="xLandscapeBtn" ButtonType="ImageButton" HorizontalAlignment="Left" VerticalAlignment="Bottom" ButtonFontImageSize="23" ButtonImageType="Mobile" Click="xOrientationBtn_Click" ToolTip="Switch to Landscape" Margin="10,0,0,0" RenderTransformOrigin="0.5,0.5" Visibility="Collapsed"  >
            <UserControls:ucButton.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="90"/>
                    <TranslateTransform/>
                </TransformGroup>
            </UserControls:ucButton.RenderTransform>
        </UserControls:ucButton>

        <!--Main area mobile image-->
        <Grid Grid.Column="0" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition  Width="10"/>
                <ColumnDefinition  Width="100*"/>
                <ColumnDefinition  Width="10"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Background="Black" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
            <Canvas x:Name="xDeviceScreenshotCanvas" Grid.Column="1" Grid.Row="0" Background="#424242" >
                <Image x:Name="xDeviceScreenshotImage" Height="{Binding ActualHeight, ElementName=xDeviceScreenshotCanvas}" Width="{Binding ActualWidth, ElementName=xDeviceScreenshotCanvas}"  
                   MouseLeftButtonDown="xDeviceScreenshotImage_MouseLeftButtonDown" 
                   MouseMove="xDeviceScreenshotImage_MouseMove" 
                   MouseLeftButtonUp="xDeviceScreenshotImage_MouseLeftButtonUp" 
                   MouseEnter="xDeviceScreenshotImage_MouseEnter" 
                   MouseLeave="xDeviceScreenshotImage_MouseLeave"  
                   SizeChanged="xDeviceScreenshotImage_SizeChanged">
                </Image>
            </Canvas>
            <StackPanel Grid.Column="2" Background="Black" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        </Grid>

        <!--Main area bottom buttons-->
        <Frame x:Name="xMainBottomFrame" Grid.Row="2" Grid.Column="0" Margin="0,0,0,0" Background="Black"/>
        <UserControls:ucButton x:Name="xBackButton" Grid.Column="0" Grid.Row="2"  ButtonType="ImageButton" HorizontalAlignment="Right" VerticalAlignment="Center" ButtonFontImageSize="16" ButtonImageType="Triangle" Click="xBackBtn_Click" ToolTip="Back" Margin="0,0,10,0"/>
        <UserControls:ucButton x:Name="xHomeBtn" Grid.Column="0" Grid.Row="2"  ButtonType="ImageButton" HorizontalAlignment="Center" VerticalAlignment="Center" ButtonFontImageSize="16" ButtonImageType="Circle" Click="xHomeBtn_Click" ToolTip="Home" Margin="0,0,0,0"/>
        <UserControls:ucButton x:Name="xMenuBtn" Grid.Column="0" Grid.Row="2"  ButtonType="ImageButton" HorizontalAlignment="Left" VerticalAlignment="Center" ButtonFontImageSize="16" ButtonImageType="Square" Click="xMenuBtn_Click" ToolTip="Menu" Margin="10,0,0,0"/>

        <!--Configurations area-->
        <Frame x:Name="xConfigurationsFrame" Grid.Row="0" Grid.Column="1" Grid.RowSpan="3" Margin="0,0,0,0" Background="{StaticResource $BackgroundColor_LightGray}"/>
        <GridSplitter x:Name="xConfigurationsSplitter" Grid.Column="0" Grid.Row="0" ResizeDirection="Columns"  Grid.RowSpan="3" HorizontalAlignment="Right" VerticalAlignment="Stretch" Width="1" Background="{StaticResource $BackgroundColor_DarkGray}"/>
        <!--<Label Grid.Column="2" Grid.Row="0" Content="Configurations" Style="{StaticResource $HeaderStyle}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>-->
        <ScrollViewer Grid.Column="1" Grid.Row="1">
            <StackPanel >
                <Label Content="Device View:" Style="{StaticResource $LabelStyle}" FontWeight="Bold" Margin="0,10,0,0"></Label>
                <CheckBox x:Name="xTrackActionsChK"  Content="Auto refresh after Action run" IsChecked="True"  VerticalAlignment="Top" HorizontalAlignment="Left" Margin="5,0,0,0" Style="{StaticResource $CeckBoxStyle}"/>
                <StackPanel Orientation="Horizontal">
                    <Label Content="Refresh waiting rate:" Style="{StaticResource $LabelStyle}" Margin="0,5,10,0"></Label>
                    <ComboBox x:Name="xRefreshWaitingRateCombo" Style="{StaticResource $FlatEditInputComboBoxStyle}" Width="35">
                        <ComboBoxItem>0</ComboBoxItem>
                        <ComboBoxItem IsSelected="True">1</ComboBoxItem>
                        <ComboBoxItem>2</ComboBoxItem>
                        <ComboBoxItem>3</ComboBoxItem>
                        <ComboBoxItem>4</ComboBoxItem>
                        <ComboBoxItem>5</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
