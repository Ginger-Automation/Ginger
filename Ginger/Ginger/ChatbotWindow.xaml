<UserControl x:Class="Ginger.ChatbotWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                          xmlns:usercontrols="clr-namespace:Amdocs.Ginger.UserControls"

        Height="400" Width="300" Background="White">
    <Grid>
        <ScrollViewer>
            <StackPanel x:Name="chatPanel">
                <!-- Dummy chat messages -->
                <TextBlock Text="Bot: Hello!" Margin="5"/>
                <TextBlock Text="You: Hi there!" Margin="5"/>
            </StackPanel>
        </ScrollViewer>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="5">
            <!-- User input -->
            <TextBox x:Name="txtInput" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Width="200" Margin="5"/>
            <!-- Send icon button -->
            <Button Click="SendMessage" Width="50" Height="32" Margin="5">
                <usercontrols:ImageMakerControl x:Name="xSend" Grid.Row="1" ImageType="SendIcon" Width="50" Foreground="LightGray" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
