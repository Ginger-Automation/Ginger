name: CD

on:
    workflow_dispatch: 

jobs:
    CI:
        name: CI
        uses: nadeemjazmawe/Ginger/.github/workflows/main.yml@master
     
    Release:
        name: Release
        needs: CI
        uses: nadeemjazmawe/Ginger/.github/workflows/Release.yml@master
     
    Deploy:
        name: Deploy
        needs: Release
        uses: nadeemjazmawe/Ginger/.github/workflows/Deploy.yml@master
        
        
