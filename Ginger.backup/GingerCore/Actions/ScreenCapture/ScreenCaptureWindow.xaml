    <Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="GingerCore.Actions.ScreenCapture.ScreenCaptureWindow"
        MouseDown="Window_MouseDown"
        MouseUp="Window_MouseUp"
        MouseMove="Window_MouseMove"
        MouseDoubleClick="Window_DClick"
        Title="ScreenCaptureWindow" WindowState="Maximized" 
     AllowsTransparency="True" WindowStyle="None"
    Topmost="True">
    
    <Window.Background>
        <SolidColorBrush Opacity="0.1" Color="Green"/>
    </Window.Background>
    <Canvas
            x:Name="dragSelectionCanvas"
            Visibility="Collapsed"
            >
        <Border 
                x:Name="dragSelectionBorder"
                BorderBrush="Blue"
                BorderThickness="1"
                Background="LightBlue"
                CornerRadius="1"
                Opacity="0.5"
                />
    </Canvas>
</Window>
